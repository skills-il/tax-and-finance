{
  "cycles": [
    {
      "version": "1.2.1",
      "date": "2026-06-07",
      "lessons": [
        "API surface verified against grow-il.readme.io. One CRITICAL fix: the transactionTypes payment-method table used fabricated numeric value-codes (1=CC, 5=PayBox, 6=Bit, 13=Apple Pay, 14=Google Pay, 15=Bank Transfer). The real API uses FIXED positional array indices (0 CC, 1 Bit, 2 Apple Pay, 3 Google Pay, 4 Bank transfer, 5 Pay Box) and is SDK-wallet-only. Fixed in SKILL.md, SKILL_HE.md, and references/api-endpoints.md.",
        "Everything else verified correct: base URLs (sandbox.meshulam.co.il / secure.meshulam.co.il), /api/light/server/1.0/ path, all endpoint names, multipart/form-data + server-side-only rule, createPaymentProcess required params, the mandatory approveTransaction step, and webhook auth via webhookKey field (NOT an X-Grow-Signature header).",
        "Built evidence.json from scratch (legacy skill). Could not live-test the API (payment endpoint, needs merchant credentials); relied on the official readme docs.",
        "Minor deferred: webhook payload also exposes statusCode / transactionToken / allPaymentNum fields the skill omits; add if a webhook-field expansion is needed next cycle."
      ]
    },
    {
      "version": "1.3.0",
      "date": "2026-07-18",
      "lessons": [
        "Docs domain moved grow-il.readme.io -> developers.grow.business (301, identical paths). Swapped the canonical host across SKILL.md/HE/references/evidence. The docs PORTAL is now labeled v2.0 but the Light API path is UNCHANGED at /api/light/server/1.0/ - do NOT bump the path.",
        "MAJOR: added the webhook fields the integration actually depends on and that were omitted: statusCode (2 = paid, the server-side success check), transactionToken (saved card token for recurring, with a 'where the token comes from' note in Step 9), allPaymentNum (allPaymentsNum in newer PaymentLinks payloads). Corrected the prior evidence snippet 'processToken' -> 'transactionToken' for the token field.",
        "CRITICAL (Judge, pre-existing fabrication): createTransactionWithToken examples used `token`/`paymentType=1`/`isRecurringDebitId=1` - all wrong. Verified against the live reference and fixed to `cardToken` / `paymentType=2` (Regular) / `recurringDebitId` (the value returned by the first premium-recurring payment; there is no isRecurringDebitId flag). The endpoint uses `userId` not `pageCode`. Added the full required-params set + an evidence claim. Fixed in SKILL.md, SKILL_HE.md, and scripts/grow-payment-helper.sh. Lesson: an API-wrapper's copy-paste curl params are the highest-risk fabrication surface - verify every param name against the live reference page, not memory.",
        "MAJOR (Expert): the server-to-server notifyUrl callback NESTS its fields under a `data` object ({err, status, data}); a dev reading req.body.statusCode gets undefined. Added a nesting note so statusCode/transactionToken/transactionId are read as data.* on that callback, distinct from the flatter webhookKey webhook system.",
        "Deferred: settleSuspendedTransaction params (live docs list userId/transactionId/transactionToken/sum vs the skill's pageCode/transactionId) - reconcile next cycle; createFarPaymentRequest endpoint not covered (one-line mention); re-verify the apisupport@grow.business support email against the live account portal."
      ]
    }
  ]
}
